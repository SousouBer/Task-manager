@props(['link'])

<div class="self-start">
    <a href="{{ $link }}" class="flex items-center justify-center gap-4">
        <img src="{{ asset('/images/Vector.png') }}" alt="Back icon" />BACK</a
    >
</div>
